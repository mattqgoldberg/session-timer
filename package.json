{
  "name": "session-timer",
  "version": "1.0.0",
  "description": "A simple web app that logs work sessions with categories and statistics.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "vite",
    "preview": "vite preview"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0"
  }
}
